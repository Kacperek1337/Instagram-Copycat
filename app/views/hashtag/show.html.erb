<div class="container text-center">
    <h1>#<%= @hashtag.name %></h1>
    <h4 class="text-secondary"><%= pluralize @hashtag.posts.count, 'Post' %><h4>
    <hr>
    <%= render partial: 'shared/post', collection: @hashtag.posts %>
</div>