<div class="container text-center">
    <% if @hashtags.count.zero? %>
        <h1>No results for "<%= @query %>"</h1>
    <% else %>
        <h1>Search Results for "<%= @query %>"
    <% end %>
    <hr>
    <%= render partial: 'hashtag', collection: @hashtags %>
</div>